@model KollamAutoEng_web.Models.VehicleModel

@{
    ViewData["Title"] = "Create Vehicle Model";
}

<head>
    <link rel="stylesheet" href="~/css/site.css" />
</head>

<body class="crud-body">
    <div class="view d-flex align-items-center justify-content-center">
        <div class="create-form">
            <h1 class="text-center">Create New Vehicle Model</h1>
            <hr />
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group row">
                    <label asp-for="ModelName" class="col-sm-4 col-form-label"></label>
                    <div class="col-sm-8">
                        <input asp-for="ModelName" class="form-control" />
                        <span asp-validation-for="ModelName" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group row">
                    <label asp-for="BrandId" class="col-sm-4 col-form-label"></label>
                    <div class="col-sm-8">
                        <select asp-for="BrandId" class="form-control" asp-items="ViewBag.BrandId"></select>
                    </div>
                </div>

                <div class="form-group row text-center">
                    <div class="col-sm-6">
                        <input type="submit" value="Create" class="create-button btn-block" />
                    </div>
                    <div class="col-sm-6">
                        <a asp-action="Index" class="back-button btn-block">Back to List</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
