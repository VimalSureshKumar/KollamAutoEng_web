@model IEnumerable<KollamAutoEng_web.Models.Supervisor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Supervisor_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supervisor_Phone_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supervisor_Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supervisor_Pay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supervisor_Hours)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Supervisor_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supervisor_Phone_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supervisor_Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supervisor_Pay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supervisor_Hours)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SupervisorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SupervisorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SupervisorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
