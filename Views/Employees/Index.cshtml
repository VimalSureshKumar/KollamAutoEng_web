@model IEnumerable<KollamAutoEng_web.Models.Employees>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employee_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee_Phone_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee_Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee_Pay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee_Hours)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employee_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee_Phone_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee_Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee_Pay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee_Hours)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
