@model KollamAutoEng_web.Models.FaultPart

@{
    ViewData["Title"] = "Edit Fault Part";
}

<body class="crud-body">
    <div class="view d-flex align-items-center justify-content-center">
        <div class="create-form text-center">
            <h1>Edit Fault Part</h1>
            <hr />
            <div class="edit-form">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="FaultPartId" />
                    <div class="form-group row">
                        <label asp-for="FaultId" class="col-form-label col-md-4 text-md-right">Fault</label>
                        <div class="col-md-8">
                            <select asp-for="FaultId" class="form-control" asp-items="ViewBag.FaultId"></select>
                            <span asp-validation-for="FaultId" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label asp-for="PartId" class="col-form-label col-md-4 text-md-right">Part</label>
                        <div class="col-md-8">
                            <select asp-for="PartId" class="form-control" asp-items="ViewBag.PartId"></select>
                            <span asp-validation-for="PartId" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label asp-for="AppointmentId" class="col-form-label col-md-4 text-md-right">Appointment</label>
                        <div class="col-md-8">
                            <select asp-for="AppointmentId" class="form-control" asp-items="ViewBag.AppointmentId"></select>
                            <span asp-validation-for="AppointmentId" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label asp-for="CustomerId" class="col-form-label col-md-4 text-md-right">Customer</label>
                        <div class="col-md-8">
                            <select asp-for="CustomerId" class="form-control" asp-items="ViewBag.CustomerId"></select>
                        </div>
                    </div>
                    <div class="form-group row">
                        <label asp-for="VehicleId" class="col-form-label col-md-4 text-md-right">Vehicle</label>
                        <div class="col-md-8">
                            <select asp-for="VehicleId" class="form-control" asp-items="ViewBag.VehicleId"></select>
                        </div>
                    </div>
                    <div class="form-group row text-center">
                        <div class="col-md-6">
                            <input type="submit" value="Save" class="create-button" />
                        </div>
                        <div class="col-md-6">
                            <a asp-action="Index" class="back-button">Back to List</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
