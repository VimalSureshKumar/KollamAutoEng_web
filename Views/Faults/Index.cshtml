@model IEnumerable<KollamAutoEng_web.Models.Fault>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">Faults</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<form asp-controller="Faults" asp-action="Index">
    <p>
        Search Faults: <input type="text" name="SearchString" />
        <input type="submit" value="Search" />
    </p>
</form>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vehicle.Registration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.FirstName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FaultId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FaultId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FaultId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
